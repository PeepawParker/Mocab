const express = require("express");

const quizController = require("../controllers/gptController");

const router = express.Router();

// router.get("/quiz/session", quizController.createSessionDefQuiz);

module.exports = router;
